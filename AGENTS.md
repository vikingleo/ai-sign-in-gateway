# AGENTS.md - 仓库执行约束

本文件定义 AI 辅助开发在 `ai-sign-in-gateway` 仓库内必须遵守的规则。所有结论以当前代码、测试、日志和版本控制证据为准。

## 1. 核心原则

- 全程使用中文沟通，回答直接收束当前诉求，不附加无关建议。
- 事实优先：先读取代码、文档、测试结果、运行日志或 git 状态，再下结论。
- Debug First：禁止为了“先跑通”添加隐藏回退、静默容错、模拟成功路径或吞没异常。失败必须显式暴露，便于定位根因。
- 先保证核心行为等价与输入输出正确，再讨论性能优化、视觉微调或基准测试。
- 当前仓库已有未提交改动时，默认视为用户改动；不得回滚、覆盖或顺手整理无关文件。

## 2. 项目事实

- 后端：Go 1.25、chi、GORM、纯 Go SQLite、JWT 管理端认证。
- 前端：Vue 3、Vite、TypeScript、Ant Design Vue。
- 运行与部署：单文件 release、Docker、源码开发均支持；Docker 默认端口为 `8972`。
- 默认数据库：SQLite。Docker 中数据库路径为 `/app/data/ai-sign-in-gateway.db`，volume 名为 `ai-sign-in-gateway-data`。
- 管理端 API、静态资源和 OpenAI 兼容网关由同一服务提供；统一网关入口为 `/api/gateway`。
- 常用文档入口：`README.md`、`docs/DEVELOPMENT.md`、`docs/DEPLOYMENT.md`、`docs/USAGE.md`。

## 3. 任务工作流

- 当前未发现标准化任务跟踪文件。若用户要求按任务循环开发，必须先创建或确认 `tasks.md` 或 `issues.csv`，再开始原子任务。
- `docs/开发进度与下一步.md` 只作为当前状态参考，不作为唯一任务驱动源。
- 每次只处理一个原子任务。流程为：读取上下文，锁定边界，开发，验证，自审，记录结果。
- 若任务标题包含 `[Code Review]`，切换为严格审计模式：先读 `git diff` 和目标文件，再执行可用类型检查与测试，默认不修改业务代码；审计结果写入 `docs/reviews/`。
- 开始任务前检查是否存在 `docs/skills/`。若存在且命中当前场景，必须阅读对应 `SKILL.md` 并声明使用的技能。

## 4. 开发边界

- 只修改当前任务所需文件，不顺手修复无关问题。
- 禁止为迎合测试硬编码假设；验收标准必须来自业务规则和代码事实。
- 业务逻辑优先通过依赖注入表达，不在核心流程中硬编码具体实现。
- 外部输入必须在边界处校验，数据库访问必须使用参数化查询或 ORM 安全接口。
- 严禁把密钥、token、密码写入源码。用户临时粘贴密钥用于调试不等同于源码泄漏；调试后不得回显密钥。
- 代码、注释、日志字符串和 Markdown 文档禁止使用 emoji、Unicode 装饰符号或非 ASCII 装饰字符。中文正文允许使用，列表使用 `-` 或数字列表。

## 5. 质量基线

- 函数尽量不超过 50 行，文件尽量不超过 300 行；超出时按职责拆分。
- 嵌套深度控制在 3 层以内，优先使用卫语句和提前返回。
- 位置参数超过 3 个时优先改为配置对象或结构体。
- 避免 magic number，提取有业务含义的命名常量。
- 注释只解释非直观意图，不复述代码表面行为。
- 前端 UI 修改必须保持后台工具属性：信息密度合理、控件稳定、不出现文本重叠、移动端和桌面端都可用。

## 6. 验证命令

按改动范围选择最小充分验证，不用主观判断替代测试。

后端：

```bash
go test ./...
go build ./...
go vet ./...
```

前端：

```bash
cd frontend
npm ci
npm run build
```

Docker：

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f app
```

基础运行检查：

```bash
curl -fsS http://127.0.0.1:8972/api/health
```

若测试依赖真实站点、真实模型、真实 SQLite schema、Docker volume 或浏览器登录态，必须明确说明验证覆盖了什么，未覆盖什么。不得把 fake、stub 或纯内存语义测试宣称为真实环境通过。

## 7. 数据与 Schema 规则

- 处理 CSV、JSON、数据库 schema、API DTO 时，必须明确定义字段顺序、类型、含义和空值行为。
- 业务逻辑依赖真实数据库列类型、SQL 类型转换、驱动序列化或运行时迁移时，至少补充离线 schema 契约测试。
- 若当前环境无法连接真实数据库或运行真实容器，必须在验证记录中标明“已覆盖语义，未覆盖真实 schema 或真实运行环境”。
- 修改运行时迁移、SQLite 字段或认证表结构时，优先增加针对旧库升级路径的测试或手动验证记录。

## 8. Git 与文件卫生

- 修改前后都要查看 `git status --short`，避免混入无关改动。
- 提交前运行 `git diff --name-only`，确认仅包含当前任务边界内文件。
- 每个原子任务单独提交，提交信息说明改动内容和验证结果。
- 默认不提交日志、临时文件、构建产物、个人配置和本地数据库。
- 本仓库远端模式为个人 fork `origin` 与上游 `upstream`；不得向 `upstream` 推送。

## 9. 常见项目踩坑

- Docker 环境变量中的 `SECRET_KEY`、`GATEWAY_API_KEY`、`DEFAULT_ADMIN_PASSWORD` 只是示例值，生产必须修改。
- 默认管理员密码只在首次初始化数据库时写入；已有 SQLite 数据库不会因环境变量变化自动重置密码。
- 修改前端后必须执行 `npm run build`；单文件 release 或 Docker 镜像需要重新构建才能加载新前端产物。
- 修改 Go 代码后必须重新构建或重启服务，否则运行中的二进制仍是旧逻辑。
- 网关转发、模型列表、图片生成和供应商站点验证必须区分连通性、鉴权、余额、模型不存在、上游超时和本地网关错误。
